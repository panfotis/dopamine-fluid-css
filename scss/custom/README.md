# `scss/custom/`

Drop your own `.scss` partials here. Each file is compiled to a matching `.css` in `css/custom/` by `npm run sass`. Use this for project-specific styles that sit alongside the generated Dopamine output.
